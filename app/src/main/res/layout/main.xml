<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <com.gwnbs.solitaire.SolitaireView
        android:id="@+id/solitaire"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center_horizontal"
            android:text="@string/solitaire_layout_text_text"
            android:visibility="visible" />

        <ImageView
            android:id="@+id/imageExpand"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:background="@drawable/menu_background"
            android:contentDescription="@null"
            android:padding="5dp"
            android:src="@drawable/ic_expand" />

        <TextView
            android:id="@+id/textLiveNotification"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginBottom="5dp"
            android:background="@drawable/dialog_background"
            android:drawableStart="@drawable/ic_lives"
            android:drawableLeft="@drawable/ic_lives"
            android:drawablePadding="5dp"
            android:padding="10dp"
            android:textSize="18sp"
            android:visibility="gone"
            app:drawableStartCompat="@drawable/ic_lives"
            tools:ignore="HardcodedText,UseCompatTextViewDrawableXml" />

    </RelativeLayout>

</FrameLayout>

